<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_main">

    <!-- Header Admin — warna emas/kuning agar berbeda -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp"
        android:background="#FF9800"
        android:elevation="4dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <Button
                android:id="@+id/btn_back_daftar_user"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:text="←"
                android:textSize="20sp"
                android:textColor="#FFFFFF"
                android:background="@android:color/transparent"/>

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="👑 Admin Panel"
                android:textSize="22sp"
                android:textStyle="bold"
                android:textColor="#FFFFFF"
                android:layout_marginStart="8dp"/>

            <!-- Jumlah user -->
            <TextView
                android:id="@+id/tv_jumlah_user"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="0 user"
                android:textSize="14sp"
                android:textColor="#FFF3E0"
                android:background="#BF360C"
                android:paddingStart="10dp"
                android:paddingEnd="10dp"
                android:paddingTop="4dp"
                android:paddingBottom="4dp"
                android:layout_marginStart="8dp"/>

        </LinearLayout>

        <!-- Subtitle -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Kelola Akun User"
            android:textSize="13sp"
            android:textColor="#FFF3E0"
            android:layout_marginTop="4dp"
            android:layout_marginStart="48dp"/>

    </LinearLayout>

    <!-- Info tabel -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp"
        android:background="#FFE0B2">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="📊 Database: tabel_user"
            android:textSize="12sp"
            android:textStyle="bold"
            android:textColor="#E65100"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Kolom: id · username · password · petType · isAdmin"
            android:textSize="11sp"
            android:textColor="#BF360C"
            android:layout_marginTop="2dp"/>

    </LinearLayout>

    <!-- Tombol Buat Quest untuk User -->
    <Button
        android:id="@+id/btn_buat_quest_panel"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:text="📋 Buat Quest untuk User"
        android:textSize="14sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:backgroundTint="#FF9800"
        android:stateListAnimator="@null"/>

    <!-- Tombol Review Bukti -->
    <Button
        android:id="@+id/btn_review_bukti"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:layout_marginTop="6dp"
        android:text="🔍 Review Bukti Quest"
        android:textSize="14sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:backgroundTint="#4CAF50"
        android:stateListAnimator="@null"/>

    <!-- RecyclerView daftar user -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_daftar_user"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp"
        android:clipToPadding="false"/>

</LinearLayout>
